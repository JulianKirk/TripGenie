name: Student 2 CI

on:
  push:
    paths:
      - 'student-2/**'
      - '.github/workflows/student-2-ci.yml'
  pull_request:
    paths:
      - 'student-2/**'
      - '.github/workflows/student-2-ci.yml'
  workflow_dispatch:

jobs:
  validate:
    name: Build and Validate Student 2
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run Tests Placeholder
        run: echo "Validating Student 2 microservices..."

[pytest]
# Import roots: repo root for `shared`, student-2 for `database`.
# ponytail: only student-2 uses top-level `database`; every student root on the
# path made the five `database` packages collide. student-1 installs its own
# packages (student-1/pyproject.toml) and students 3-5 have no tests yet --
# add a root here only when a student needs one and its package names are unique.
pythonpath = . student-2

[pytest]
# Import roots: repo root for `shared`, student-N for `database`/`backend`.
# Means tests run with a plain `pytest` from anywhere, no PYTHONPATH or .pth.
pythonpath = . student-1 student-2 student-3 student-4 student-5
